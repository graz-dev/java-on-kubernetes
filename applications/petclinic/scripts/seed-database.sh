#!/usr/bin/env bash
# This script is no longer needed.
# spring-framework-petclinic ships with pre-populated H2 sample data
# (10 owners, 13 pets) loaded at startup from data.sql — no external seeding required.
echo "seed-database.sh is obsolete for spring-framework-petclinic."
echo "H2 sample data (owners 1-10, pets 1-13) is pre-loaded at pod startup."
exit 0
